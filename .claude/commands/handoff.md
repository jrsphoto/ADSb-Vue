---
description: Summarize this session for a fresh session to pick up mid-task
---

I am about to run out of context. Produce a handoff block I can paste into a
new session. Include only what a fresh session cannot derive from the repo:

1. What we are in the middle of, and which files are partially modified.
2. The mental model behind the approach, especially anything non-obvious about
   why it is structured this way.
3. What has already been ruled out, with reasons.
4. The exact next action.

Do not summarize the codebase. Do not restate CLAUDE.md. Assume the new session
will run `/preflight` first.
