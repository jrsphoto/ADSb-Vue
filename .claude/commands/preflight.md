---
description: Verify the environment and load current project state
---

Run `./scripts/preflight.sh` and report the result concisely.

If any check fails, stop and tell me what is broken. Do not begin work and do
not attempt to work around a failed check by assuming a host is reachable.

If everything passes, read `STATUS.md` and give me a three-line summary: the
current objective, the next concrete step, and anything listed as blocked.
