# Decisions

Append-only. One short entry per non-obvious choice, so that neither you nor a
future session relitigates it. Date, decision, and the reason that would not be
guessable from the code.

## YYYY-MM-DD - Example: chose X over Y

Y was the obvious choice but it does Z under load, which we measured at N.
X costs more in M but does not have that failure mode. Revisit if M becomes
the bottleneck.
